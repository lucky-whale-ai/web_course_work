{
  "name": "roof-profi-coursework",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "api": "node server/local.mjs",
    "build": "vite build && node scripts/build-worker.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": { "json-server": "0.17.4" },
  "devDependencies": { "vite": "6.1.0", "esbuild": "0.25.0", "drizzle-kit": "0.30.4", "drizzle-orm": "0.39.3" }
}
